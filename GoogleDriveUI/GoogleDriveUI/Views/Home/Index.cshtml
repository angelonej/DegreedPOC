
<html ng-app="tree">
<head>
    <link rel="stylesheet" href="content/css/tree.css" />
</head>

<body>


    <h2>GoogleDrive Folders</h2>

    <div ng-controller="TreeController as tc">
        <ul class="tree">
            <node-tree children="tc.tree"></node-tree>
        </ul>
    </div>
    <script src="content/lib/angular.min.js"></script>
    <script src="content/src/Controllers/TreeController.js"></script>
    <script src="content/src/Services/TreeService.js"></script>
    <script src="content/src/Config/URLConfig.js"></script>
    <script src="content/src/Directives/TreeDirectives.js"></script>



    @*<div>
        <h2>All Folder</h2>
        <ul id="folders" />
    </div>
    <div>
        <h2>Search by ID</h2>
        <input type="text" id="folderId" size="15" />
        <input type="button" value="Search" onclick="find();" />
        <p id="folder" />
    </div>

    <script src="http://ajax.aspnetcdn.com/ajax/jQuery/jquery-2.0.3.min.js"></script>
    <script>
        var uri = 'api/googledrive';

        $(document).ready(function () {
         

            $.getJSON(uri)
                .done(function (data) {
                 
                    $.each(data, function (key, item) {
              
                        $('<li>', { text: item.Name }).appendTo($('#folders'));
                    });
                });
        });



        function find() {
            var id = $('#folderId').val();

            $.getJSON(uri + '/' + id)
                .done(function (data) {
                    $('#folder').text(data.Name);
                })
                .fail(function (jqXHR, textStatus, err) {
                    $('#folder').text('Error: ' + err);
                });
        }
    </script>*@
</body>
</html>